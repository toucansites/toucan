{{<index}}

{{$main}}

<div id="posts" class="wrapper">
    {{#page.context.tag}}
    <header>
        <div class="profile-picture">
            {{> _partials.figure}}
        </div>
        <h1>{{title}}</h1>
        <p>{{description}}</p>
        <p>Posts: {{numberOfPosts}}</p>
    </header>
    {{/page.context.tag}}


    {{& page.content}}
    
    
    {{#empty(page.context.posts)}}
    Empty.
    {{/empty(page.context.posts)}}
    {{^empty(page.context.posts)}}
    <div class="grid">
    {{#page.context.posts}}
        {{> _partials.post}}
    {{/page.context.posts}}
    </div>
    {{/empty(page.context.posts)}}
</div>

{{/main}}

{{/index}}
