        <a class="relatedPosts" href="{{permalink}}">
            <div class="relatedCover">{{#imageUrl}}<img src="{{imageUrl}}" alt="{{title}}">{{/imageUrl}}</div>
            <div class="relatedContent">
                <h3 class="relatedTitle">{{title}}</h3>
                <p>{{description}}</p>
            <div class="relatedAuthors">
            {{#authors}}
                <div class="authorBadge">
                    <div class="mini">
                        {{#imageUrl}}<img src="{{imageUrl}}" alt="{{title}}">{{/imageUrl}}
                    </div>
                    <div>{{title}}</div>
                </div>
            {{/authors}}
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-dot" viewBox="0 0 16 16">
                    <path d="M8 9.5a1.5 1.5 0 1 0 0-3 1.5 1.5 0 0 0 0 3"/>
                </svg>  
                <time class="RelatedDate" datetime="{{date}}">{{date}}</time>
            </div>
            </div>
        </a>
