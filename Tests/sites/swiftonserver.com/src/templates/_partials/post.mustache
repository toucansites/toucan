<div class="post">
    <a href="{{permalink}}" target="">
        {{> blocks.figure}}
        <section>
            <time datetime="{{date}}">{{date}}</time>
            <h2 class="title">{{title}}</h2>
        </section>
        <p>{{excerpt}}</p>
    </a>
</div>
