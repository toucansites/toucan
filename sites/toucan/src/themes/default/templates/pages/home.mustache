{{<index}}
{{$main}}

{{& page.content}}

<div id="home" class="wrapper">
    <h2>Pages</h2>
    {{#empty(page.context.pages)}}
    Empty.
    {{/empty(page.context.pages)}}
    {{^empty(page.context.pages)}}
    <ul>
    {{#page.context.pages}}
    <li>
        <a href="{{permalink}}">{{title}}</a>
    </li>
    {{/page.context.pages}}
    </ul>
    {{/empty(page.context.pages)}}
</div>
{{/main}}
{{/index}}

