{{<index}}
{{$main}}

{{userDefined.foo.bar}}

<br>

{{#page.data}}
    {{name}}<br>
    {{desc}}<br>
    ---<br><br>
    {{.}}<br>
{{/page.data}}

<div id="author-page" class="content wrapper">
    {{#page.context.author}}
    <header>
        {{#imageUrl}}<img class="large rounded" src="{{imageUrl}}" alt="{{title}}">{{/imageUrl}}
        <br><br>
        <h1>{{title}}</h1>
        <hr>
        <p>{{description}}</p>
        <p>{{numberOfPosts}} posts</p>
    </header>
    {{/page.context.author}}
    
    {{& page.content}}

    {{#empty(page.context.posts)}}
    Empty.
    {{/empty(page.context.posts)}}
    {{^empty(page.context.posts)}}
    <div class="grid grid-321">
    {{#page.context.posts}}
        {{> blog.blocks.post}}
    {{/page.context.posts}}
    </div>
    {{/empty(page.context.posts)}}
    
</div>

{{/main}}
{{/index}}
