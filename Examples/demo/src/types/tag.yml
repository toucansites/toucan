id: tag

properties:
    order:
        type: int
        required: true


context:
    site:
        tags:
            sort: title
            order: asc
            pagination:
                limit: 10        

template: blog.single.tag
