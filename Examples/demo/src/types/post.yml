id: post

properties:
    - order

context:
    site:
        posts:
            sort: title
            order: asc
            pagination:
                limit: 10
        

template: blog.single.post
