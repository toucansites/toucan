id: category

properties:
    - order

context:
    site:
        categories:
            sort: title
            order: asc
            pagination:
                limit: 10

    page:
        guides:
            references: guide
            using: category
            join: one
            limit: 5
            sort: order
            order: asc

        

template: docs.single.category
