{{<html}}
{{$main}}

{{& page.contents}}

<div id="posts" class="wrapper">

    {{#empty(page.posts)}}
    Empty.
    {{/empty(page.posts)}}
    {{^empty(page.posts)}}
    <div class="grid grid-321">
    {{#page.posts}}
        {{> blog.blocks.post}}
    {{/page.posts}}
    </div>
    {{/empty(page.posts)}}
    
    {{#empty(page.pagination)}}
    {{/empty(page.pagination)}}
    {{^empty(page.pagination)}}
    
    <div class="pagination">
    {{#page.pagination}}
        {{> blocks.pagination}}
    {{/page.pagination}}
    </div>
    {{/empty(page.pagination)}}

</div>

{{/main}}
{{/html}}
