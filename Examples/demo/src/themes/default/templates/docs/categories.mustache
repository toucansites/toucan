{{<index}}
{{$main}}
<div id="docs">
    <div class="left">
        {{> docs.blocks.categories }}
    </div>
    <div class="center">
        <article>
            {{& page.content}}

            {{#empty(page.context.categories)}}
            Empty.
            {{/empty(page.context.categories)}}
            {{^empty(page.context.categories)}}
            <div>
            <ul>
            {{#page.context.categories}}
                <li>
                    <a href="/{{slug}}">{{title}}</a>
                </li>
            {{/page.context.categories}}
            </ul>
            </div>
            {{/empty(page.context.categories)}}

        </article>
    </div>
    <div class="right">
        {{> blocks.toc }}
    </div>
</div>
{{/main}}
{{/index}}
