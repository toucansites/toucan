{{<html}}
{{$main}}
<div id="docs">
    <div class="left">
        {{> docs.blocks.categories }}
    </div>
    <div class="center">
        <article>
            {{#context.guide.category}}
            <a href="{{permalink}}">{{title}}</a>
            {{/context.guide.category}}
            
            {{& context.contents}}


            <section>
            <div class="grid grid-2">
            {{^context.guide.prev}}
            <div></div>
            {{/context.guide.prev}}
            {{#context.guide.prev}}
            <div class="prev">
                <h4>&larr; Prev guide</h4>
                <small>{{category.title}}</small>
                <a href="{{permalink}}">{{title}}</a>
            </div>
            {{/context.guide.prev}}
                
            {{^context.guide.next}}
            <div></div>
            {{/context.guide.next}}
            {{#context.guide.next}}
            <div class="next">
                <h4 style="text-align: right;">Next guide &rarr;</h4>
                <small>{{category.title}}</small>
                <a href="{{permalink}}">{{title}}</a>
            </div>
            {{/context.guide.next}}
            </div>
            </section>

        </article>
    </div>
    <div class="right">
        {{> blocks.toc }}
    </div>
</div>
{{/main}}
{{/html}}
