<aside>
    {{#empty(page.categories)}}
    Empty.
    {{/empty(page.categories)}}
    {{^empty(page.categories)}}
    <ul>
    {{#page.categories}}
        <li class="category">
            <a href="{{permalink}}">{{title}}</a>
            <ul>
            {{#guides}}
                <li><a href="{{permalink}}">{{title}}</a></li>
            {{/guides}}
            </ul>
        </li>
    {{/page.categories}}
    </ul>
    {{/empty(page.categories)}}
</aside>
