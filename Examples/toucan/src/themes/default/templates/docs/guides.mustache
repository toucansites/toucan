
{{<html}}
{{$main}}
<div id="docs">
    <div class="left">
        {{> docs.blocks.categories }}
    </div>
    <div class="center">
        <article>
            {{& page.contents}}

            {{#empty(page.categories)}}
            Empty.
            {{/empty(page.categories)}}
            {{^empty(page.categories)}}
            <ul>
            {{#page.guides}}
                <li>
                    <a href="{{permalink}}">{{title}}</a>
                </li>
            {{/page.guides}}
            </ul>
            {{/empty(page.categories)}}

        </article>
    </div>
    <div class="right">
        {{> blocks.toc }}
    </div>
</div>
{{/main}}
{{/html}}
