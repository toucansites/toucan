{{<html}}
{{$main}}
<div id="authors" class="wrapper">
    <header>
        <h1>{{page.title}}</h1>
        <p>{{page.description}}</p>
    </header>

    {{#empty(page.authors)}}
    Empty.
    {{/empty(page.authors)}}
    {{^empty(page.authors)}}
    <div class="grid grid-421">
    {{#page.authors}}
        {{> blog.blocks.author}}
    {{/page.authors}}
    </div>
    {{/empty(page.authors)}}
    
</div>
{{/main}}
{{/html}}
