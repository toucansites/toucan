<div class="card{{#featured}} featured{{/featured}}">
    {{#featured}}<span class="featured">featured</span>{{/featured}}
    {{#imageUrl}}<img src="{{imageUrl}}" alt="{{title}}">{{/imageUrl}}
    <div class="author-list">
    {{#authors}}
        <a href="{{permalink}}">
        {{#imageUrl}}<img class="small rounded" src="{{imageUrl}}" alt="{{title}}">{{/imageUrl}}
        </a>
        <a href="{{permalink}}"><span class="name">{{title}}</span></a>
    {{/authors}}
    </div>
    <div class="meta">
        
        <time datetime="{{publication.sitemap}}">{{publication.sitemap}}</time> &middot; <span>{{readingTime}} min read</span>
    </div>
    
    <h2 class="title"><a href="{{permalink}}" target="">{{title}}</a></h2>
    <p>{{description}}</p>

    <div class="tag-list">
    {{#tags}}
        <a href="{{permalink}}"><small>{{title}}</small></a>{{^last()}}<small>, </small>{{/last()}}
    {{/tags}}
    </div>
</div>
