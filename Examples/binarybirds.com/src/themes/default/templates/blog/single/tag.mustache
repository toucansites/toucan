{{<html}}
{{$main}}

<div id="tag" class="wrapper">
    
    <header>
        <div class="tag-title">
            <div class="tag-picture">{{#page.imageUrl}}<img class="medium" src="{{page.imageUrl}}" alt="{{page.title}}">{{/page.imageUrl}}</div>
            <h1>{{page.title}}</h1>
        </div>
        <hr>
        <p>{{page.description}}</p>
        <p>{{count(page.posts)}} posts</p>
    </header>

    {{& page.contents}}
    
    {{#empty(page.posts)}}
    Empty.
    {{/empty(page.posts)}}
    {{^empty(page.posts)}}
    <div class="grid grid-321">
    {{#page.posts}}
        {{> blog.blocks.post}}
    {{/page.posts}}
    </div>
    {{/empty(page.posts)}}
</div>

{{/main}}
{{/html}}
