<div class="card">
    <a href="{{permalink}}">
        <center>
        {{#imageUrl}}<img class="large rounded" src="{{imageUrl}}" alt="{{title}}">{{/imageUrl}}
        <br><br>
        <h4>{{title}}</h4>
        <p>{{count(posts)}} posts</p>
        </center>
    </a>
</div>
