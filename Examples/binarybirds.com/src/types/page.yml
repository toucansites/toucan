id: page

properties:
    order:
        type: int
        required: true


context:
    site:
        pages:
            sort: title
            order: asc
            pagination:
                limit: 10

template: pages.single.page
